# TRACE — Treatment Record Acquisition and Chronology Engine
# Runtime
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
pyjwt[crypto]>=2.9.0
cryptography>=43.0.0
httpx>=0.27.0
boto3>=1.35.0
reportlab>=4.2.0
python-dotenv>=1.0.0

# Clinical NLP (Phase 1C: provider extraction from intake transcripts)
# OpenMed >=1.7.0 adds source spans + multimodal de-identification primitives
# OpenMed full NER pipeline requires transformers + torch:
#   pip install openmed[hf]>=1.7.0,<1.9.0
openmed>=1.7.0,<1.9.0

# OCR (Phase 1D: multi-tier document text extraction)
# Tier 1B primary: Mistral OCR 4 self-hosted (Fly.io sidecar, no BAA needed)
# Tier 1B fallback: Tesseract (local, always available)
# Mistral API client for dev/staging (MISTRAL_API_KEY env var)
mistralai>=1.0.0
pytesseract>=0.3.10
pymupdf>=1.23.0
Pillow>=10.0.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0
ruff>=0.6.0
mypy>=1.11.0
