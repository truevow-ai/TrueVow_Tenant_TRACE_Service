# TRACE — Treatment Record Acquisition and Chronology Engine
# Runtime
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
pyjwt[crypto]>=2.9.0
cryptography>=43.0.0
httpx>=0.27.0
boto3>=1.35.0
reportlab>=4.2.0
python-dotenv>=1.0.0

# Clinical NLP (Phase 1C: provider extraction from intake transcripts)
# OpenMed >=1.7.0 adds source spans + multimodal de-identification primitives
# OpenMed full NER pipeline requires transformers + torch:
#   pip install openmed[hf]>=1.7.0,<1.9.0
openmed>=1.7.0,<1.9.0

# OCR engine (Phase 1D: image-based document text extraction)
# PaddleOCR-VL 1.5 (Tier 1B) — self-hosted, CPU, air-gapped.
# Replaces deepdoctection+DocTr (eliminated per ADR-003 / Tech Spec Part 2).
# numpy pinned <2 for paddle/opencv ABI compatibility.
# System deps (Dockerfile): libgl1 libglib2.0-0 libsm6 libxext6 libxrender1 libgomp1
numpy<2
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0
ruff>=0.6.0
mypy>=1.11.0
